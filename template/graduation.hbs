{{#if graduation}}
<section id="graduation">
  <header class="title">Formação</header>
  <ul class="graduation-list">
    {{#each graduation}}
    <li>
      <article class="graduation">
        <header class="graduation-title">
          {{> institutionIcon}} {{this.title}}
        </header>
        <div class="content">
          <p>Instituição: </p>
          <p>{{this.institution}}</p>
          <p>Período: </p>
          <p>{{this.period}}</p>
          <p>Descrição: </p>
          <p>{{this.description}}</p>
        </div>
      </article>
    </li>
    {{/each}}
  </ul>
</section>
{{/if}}